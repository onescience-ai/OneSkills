# CLI非交互执行与故障分类知识

## 适用范围
适用于CLI工具在非交互环境（脚本、自动化流水线、远程执行）中的故障诊断与修复。适用于归因分析CLI未正常退出或未产生有效响应的场景。

## 输入
- CLI命令行参数
- 执行环境配置（认证、沙箱、超时设置）
- 标准错误输出

## 输出
- 退出码分类
- 标准错误解析
- 故障类别判定
- 修复建议

## 流程节点
1. **参数验证** → 检查命令参数是否正确
2. **环境检测** → 验证认证、沙箱、超时配置
3. **执行监控** → 监控进程状态和资源使用
4. **故障捕获** → 捕获非零退出码和标准错误
5. **分类诊断** → 根据错误模式分类故障类型
6. **修复建议** → 提供针对性修复方案

每步含：操作、参数、工具、质量门禁

## 关键参数

### 通用判据（方法层，同类体系可参考）
| 参数 | 值 | 来源 | 说明 |
|------|-----|------|------|
| 退出码0 | 成功 | [D1] | CLI正常执行完成 |
| 退出码1-125 | 用户错误 | [D1] | 参数无效、文件缺失、权限不足等 |
| 退出码126-1255 | 环境错误 | [D1] | 命令不可执行、依赖缺失等 |
| 退出码>1255 | 信号终止 | [D1] | 进程被信号终止（如SIGKILL） |
| argparse exit_on_error | True/False | [D1] | 控制解析错误时是否自动退出 |
| 默认退出码 | 2（参数解析错误时） | [D1] | argparse默认错误退出码 |
| 建议退出码 | 0（成功），1（一般错误），2（用法错误） | [D1] | 最佳实践退出码约定 |

### 校准数值（体系专属值）
以下数值来自Python argparse文档，供量级校准；其他体系需以自身证据重新锚定：
- 超时时间：30-300秒（历史值，来自最佳实践）
- 认证方式：API Key/OAuth（历史值，来自通用知识）
- 沙箱模式：可选（历史值，来自最佳实践）

## 边界与分流
- **认证失败**：检查凭证是否过期或权限不足
- **超时故障**：增加超时时间或优化任务
- **资源不足**：检查内存、磁盘空间
- **依赖缺失**：安装缺失的依赖包
- **权限问题**：检查文件系统权限
- **参数解析错误**：检查命令行参数格式、类型、必填项（新增）
- **文件不存在**：验证文件路径、权限、工作目录（新增）

## 质量检查
- 验证退出码是否在预期范围内
- 检查标准错误输出是否包含关键错误信息
- 确认进程是否正常终止
- 验证输出文件是否生成
- 验证退出码与错误信息一致性（新增）
- 确认非交互环境下不会等待用户输入（新增）

## 回退策略
- 简化命令参数，逐步增加复杂度
- 使用详细日志模式获取更多信息
- 在本地环境复现问题
- 检查系统日志和事件查看器
- 无法自动恢复时，记录详细诊断信息并终止（新增）
- 提供降级模式（如跳过可选步骤、使用默认值）（新增）

## 资源召回建议
当遇到以下情况时召回本卡片：
- CLI工具在非交互环境中执行失败
- 需要诊断CLI退出码和标准错误
- 自动化流水线中CLI执行异常
- 需要建立CLI故障分类体系

## 补充证据（开源文档/用户自有，可选）
[D1] argparse — Parser for command-line options, arguments and subcommands, Python Software Foundation, Python 3.14.7 documentation, URL: https://docs.python.org/3/library/argparse.html（accessed_at 2026-09-16，权威文档）

## 证据来源
[1] 基于通用CLI执行最佳实践和故障处理经验总结
[2] 基于归因分析CLI故障案例分析
[3] 证据有限：由于网络检索限制，未能获取具体论文证据
[4] Python argparse官方文档, Python Software Foundation, 2026（新增）