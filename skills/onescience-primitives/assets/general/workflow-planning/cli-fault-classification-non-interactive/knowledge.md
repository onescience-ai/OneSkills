# CLI非交互执行故障分类与诊断

## 适用范围
面向需要在自动化流水线、科研工作流或远程执行环境中以非交互模式运行命令行工具的场景，提供系统化的故障分类框架和诊断方法。适用于任何CLI工具的启动配置、超时处理、退出码判读和标准错误分析。

## 输入
- CLI命令及参数
- 执行环境信息（操作系统、shell类型、权限）
- 超时与资源限制配置
- 预期输出格式（stdout/stderr/退出码）

## 输出
- 故障分类结果（类别、原因、严重程度）
- 诊断证据（退出码、stderr内容、信号信息）
- 恢复建议（重试策略、配置修正、降级方案）

## 流程节点
1. 命令构建与参数校验 → 2. 执行环境预检 → 3. 非交互执行与监控 → 4. 退出码分类 → 5. 标准错误解析 → 6. 故障归因 → 7. 恢复策略选择

每步含：操作、参数、工具、质量门禁

## 关键参数

### 通用判据（方法层，同类体系可参考）
| 参数 | 值 | 来源 | 说明 |
|------|-----|------|------|
| 退出码语义 | 0=成功, 1-125=用户定义错误, 126=命令不可执行, 127=命令未找到, 128+N=被信号N终止 | [D1] POSIX.1-2017 | 适用于所有POSIX兼容系统 |
| 超时检测 | 监控进程存活 + 周期性心跳检查 | 行业实践 | 非交互场景必须设置超时防止挂死 |
| stderr分离 | 错误信息必须写入stderr而非stdout | [D1] POSIX | 确保诊断信息不污染正常输出 |
| 信号处理 | SIGTERM(15)用于优雅终止, SIGKILL(9)用于强制终止 | [D1] POSIX | 先SIGTERM后SIGKILL的两阶段终止 |

### 校准数值（以下数值来自典型CLI工具实践，供量级校准；其他体系需以自身证据重新锚定）
| 参数 | 值 | 来源 | 说明 |
|------|-----|------|------|
| 典型超时范围 | 30s-300s（轻量级）, 300s-3600s（计算密集型） | 行业实践 | 根据任务复杂度调整 |
| 重试间隔 | 指数退避：1s, 4s, 16s | 行业实践 | 避免雪崩效应 |
| 最大重试次数 | 3次 | 行业实践 | 超过则判定为系统性故障 |
| stderr缓冲区 | 4KB-64KB | 行业实践 | 用于捕获错误上下文 |

## 边界与分流
- **退出码0但无预期输出**：可能为静默失败 → 检查stdout内容完整性
- **退出码126（权限不足）**：检查文件权限、沙箱策略 → 修正权限或调整沙箱配置
- **退出码127（命令未找到）**：检查PATH、conda环境、模块加载 → 补齐环境依赖
- **退出码128+N（信号终止）**：N=9(SIGKILL)为OOM或人工干预, N=11(SIGSEGV)为段错误 → 检查资源限制或代码缺陷
- **超时退出**：进程未在预期时间内完成 → 增加超时限制或优化命令性能
- **stderr有输出但退出码0**：警告信息 → 记录但不阻断流程

## 质量检查
- 退出码是否在预期范围内（0或已知错误码）
- stderr是否包含可解析的错误信息
- 超时设置是否与任务复杂度匹配
- 重试策略是否避免了无限循环

## 回退策略
- 退出码无法分类时：记录原始退出码和stderr全文，标记为"未知故障"
- 超时且无法确认进程状态时：强制SIGKILL后检查输出文件完整性
- 多次重试均失败时：降级到手动执行或跳过该步骤并记录

## 资源召回建议
当以下场景出现时召回本卡片：
- CLI工具在自动化流水线中非交互执行失败
- 需要对CLI退出码进行语义分类
- 需要设计CLI工具的超时与重试策略
- 需要区分警告(stderr)与致命错误

配套资源：`general-json-schema-report-delivery-contract`（报告输出格式验证）

## 补充证据（权威文档）
[D1] The Open Group Base Specifications Issue 7, Shell Command Language, Section 2.8.1 Exit Status for Commands. IEEE Std 1003.1-2017. URL: https://pubs.opengroup.org/onlinepubs/9699919799/utilities/V3_chap02.html (accessed 2026-09-21, 权威标准文档)
[D2] Python subprocess module documentation - subprocess.run() returncode handling. Python Software Foundation. URL: https://docs.python.org/3/library/subprocess.html (accessed 2026-09-21, 官方文档)

## 证据来源
本文档基于POSIX标准[D1]和Python官方文档[D2]中关于退出码语义、信号处理和进程管理的权威定义，结合CLI工具自动化执行的行业最佳实践综合而成。无专项论文证据，所有知识均来自权威标准文档。
