# 骨架任务：数据接入与契约核验

- domain: cfd
- 复用场景数: 100
- 实例任务数: 100

## 步骤描述（跨场景聚合去重）
- 接入AirfRANS非结构网格数据，核验样本、变量、单位、网格坐标及许可。
- 接入CFD快照矩阵与参数标签，核验样本、变量、单位、网格坐标及许可。
- 接入DNS与RANS配对湍流闭合数据，核验样本、变量、单位、网格坐标及许可。
- 接入Darcy与Navier-Stokes规则网格数据，核验样本、变量、单位、网格坐标及许可。
- 接入DeepCFD规则网格翼型RANS数据，核验样本、变量、单位、网格坐标及许可。
- 接入DrivAerNet与DrivAerNet++汽车数据，核验样本、变量、单位、网格坐标及许可。
- 接入OpenFOAM与FEM案例及运行日志，核验样本、变量、单位、网格坐标及许可。
- 接入PDEBench与PINNacle多方程基准，核验样本、变量、单位、网格坐标及许可。
- 接入PDE状态控制目标轨迹，核验样本、变量、单位、网格坐标及许可。
- 接入PDE观测与参数配对数据，核验样本、变量、单位、网格坐标及许可。
- 接入PINN基准方程与训练诊断数据，核验样本、变量、单位、网格坐标及许可。
- 接入Poisson及稀疏线性系统数据，核验样本、变量、单位、网格坐标及许可。
- 接入RANS方程与稀疏均值流湍流数据，核验样本、变量、单位、网格坐标及许可。
- 接入Rayleigh-Benard对流与标量混合数据，核验样本、变量、单位、网格坐标及许可。
- 接入WindsorML与DrivAerStar与CarBench汽车CFD数据，核验样本、变量、单位、网格坐标及许可。
- 接入三维DNS与LES体湍流数据，核验样本、变量、单位、网格坐标及许可。
- 接入三维机翼与融合翼身气动数据，核验样本、变量、单位、网格坐标及许可。
- 接入三维汽车表面与体流场数据，核验样本、变量、单位、网格坐标及许可。
- 接入不可压层流Navier-Stokes配置与配点，核验样本、变量、单位、网格坐标及许可。
- 接入二维湍流DNS滤波与LES数据，核验样本、变量、单位、网格坐标及许可。
- 接入亚声速至超声速可压缩绕流数据，核验样本、变量、单位、网格坐标及许可。
- 接入任意几何非结构网格CFD数据，核验样本、变量、单位、网格坐标及许可。
- 接入低高保真配对气动数据，核验样本、变量、单位、网格坐标及许可。
- 接入具有旋转平移群对称的PDE数据，核验样本、变量、单位、网格坐标及许可。
- 接入函数输入到解函数配对数据，核验样本、变量、单位、网格坐标及许可。
- 接入分区PDE配点与界面条件数据，核验样本、变量、单位、网格坐标及许可。
- 接入动态图与拉格朗日粒子时序数据，核验样本、变量、单位、网格坐标及许可。
- 接入参数化翼型多工况与多保真数据，核验样本、变量、单位、网格坐标及许可。
- 接入参数条件与流场分布样本，核验样本、变量、单位、网格坐标及许可。
- 接入可微数值轨迹与算子数据，核验样本、变量、单位、网格坐标及许可。
- 接入叶轮高升力与工程LES及DNS数据，核验样本、变量、单位、网格坐标及许可。
- 接入含噪观测与PDE先验数据，核验样本、变量、单位、网格坐标及许可。
- 接入圆柱与钝体OpenFOAM传感动作轨迹，核验样本、变量、单位、网格坐标及许可。
- 接入圆柱绕流非定常时序数据，核验样本、变量、单位、网格坐标及许可。
- 接入壁面压力剪切与DNS体场配对数据，核验样本、变量、单位、网格坐标及许可。
- 接入复杂几何点云与表面采样数据，核验样本、变量、单位、网格坐标及许可。
- 接入复杂几何边界与无网格配点，核验样本、变量、单位、网格坐标及许可。
- 接入多孔介质微流动与输运CFD数据，核验样本、变量、单位、网格坐标及许可。
- 接入多尺度湍流粗细状态轨迹，核验样本、变量、单位、网格坐标及许可。
- 接入多尺度高频PDE场数据，核验样本、变量、单位、网格坐标及许可。
- 接入多工况翼型几何与气动力数据，核验样本、变量、单位、网格坐标及许可。
- 接入多方程多网格预训练数据，核验样本、变量、单位、网格坐标及许可。
- 接入多物理多分辨率大规模预训练数据，核验样本、变量、单位、网格坐标及许可。
- 接入多物理多功能或随机PDE数据，核验样本、变量、单位、网格坐标及许可。
- 接入多物理预训练PDE时序数据，核验样本、变量、单位、网格坐标及许可。
- 接入带守恒边界和无量纲约束PDE数据，核验样本、变量、单位、网格坐标及许可。
- 接入成对低高分辨率DNS快照，核验样本、变量、单位、网格坐标及许可。
- 接入拉格朗日粒子与涡量演化数据，核验样本、变量、单位、网格坐标及许可。
- 接入拉格朗日粒子轨迹数据，核验样本、变量、单位、网格坐标及许可。
- 接入控制体网格与守恒通量数据，核验样本、变量、单位、网格坐标及许可。
- 接入数值求解器状态与观测融合数据，核验样本、变量、单位、网格坐标及许可。
- 接入时间依赖PDE轨迹数据，核验样本、变量、单位、网格坐标及许可。
- 接入显式约束动力系统时序数据，核验样本、变量、单位、网格坐标及许可。
- 接入有限体积离散与子域界面数据，核验样本、变量、单位、网格坐标及许可。
- 接入有限标注预算的PDE设计数据，核验样本、变量、单位、网格坐标及许可。
- 接入条件流场与物理残差数据，核验样本、变量、单位、网格坐标及许可。
- 接入椭圆与演化方程变分积分数据，核验样本、变量、单位、网格坐标及许可。
- 接入槽道DNS壁面观测与吹吸控制数据，核验样本、变量、单位、网格坐标及许可。
- 接入流固耦合轨迹与目标响应数据，核验样本、变量、单位、网格坐标及许可。
- 接入涡激振动与流固耦合轨迹数据，核验样本、变量、单位、网格坐标及许可。
- 接入混合边界与复杂域PDE数据，核验样本、变量、单位、网格坐标及许可。
- 接入湍流快照与低维潜变量数据，核验样本、变量、单位、网格坐标及许可。
- 接入球面向量场与三维对称PDE数据，核验样本、变量、单位、网格坐标及许可。
- 接入百万节点通用三维PDE网格数据，核验样本、变量、单位、网格坐标及许可。
- 接入稀疏实验观测与RANS方程数据，核验样本、变量、单位、网格坐标及许可。
- 接入稀疏或缺损CFD场与坐标查询，核验样本、变量、单位、网格坐标及许可。
- 接入稀疏流场观测与未知PDE参数，核验样本、变量、单位、网格坐标及许可。
- 接入稀疏状态轨迹与导数观测，核验样本、变量、单位、网格坐标及许可。
- 接入稀疏观测与CFD先验联合数据，核验样本、变量、单位、网格坐标及许可。
- 接入粗细网格CFD成对轨迹，核验样本、变量、单位、网格坐标及许可。
- 接入缺测PDE状态视频或双帧观测，核验样本、变量、单位、网格坐标及许可。
- 接入网格误差指标与移动目标数据，核验样本、变量、单位、网格坐标及许可。
- 接入翼型几何流场性能联合数据，核验样本、变量、单位、网格坐标及许可。
- 接入翼型压力阵风载荷与控制器数据，核验样本、变量、单位、网格坐标及许可。
- 接入翼型壁面压力剪切及速度测量数据，核验样本、变量、单位、网格坐标及许可。
- 接入自适应有限元网格误差数据，核验样本、变量、单位、网格坐标及许可。
- 接入跨几何参数CFD与稀疏监测数据，核验样本、变量、单位、网格坐标及许可。
- 接入跨几何族多保真RANS数据，核验样本、变量、单位、网格坐标及许可。
- 接入跨声速超临界翼型CFD数据，核验样本、变量、单位、网格坐标及许可。
- 接入跨声速颤振与buffet耦合数据，核验样本、变量、单位、网格坐标及许可。
- 接入跨车型家族汽车CFD数据，核验样本、变量、单位、网格坐标及许可。
- 接入跨雷诺数跨几何OOD流动数据，核验样本、变量、单位、网格坐标及许可。
- 接入车辆或软体结构生成设计数据，核验样本、变量、单位、网格坐标及许可。
- 接入连续坐标与压缩潜空间PDE数据，核验样本、变量、单位、网格坐标及许可。
- 接入连续坐标采样的PDE快照，核验样本、变量、单位、网格坐标及许可。
- 接入连续时间PDE状态序列，核验样本、变量、单位、网格坐标及许可。
- 接入长时间多尺度PDE轨迹，核验样本、变量、单位、网格坐标及许可。
- 接入随机微分方程与高维PDE样本，核验样本、变量、单位、网格坐标及许可。
- 接入随机或混沌PDE时空数据，核验样本、变量、单位、网格坐标及许可。
- 接入随机或连续时间PDE分布数据，核验样本、变量、单位、网格坐标及许可。
- 接入非均匀采样与变形几何PDE数据，核验样本、变量、单位、网格坐标及许可。
- 接入非定常气动力与耦合响应时序，核验样本、变量、单位、网格坐标及许可。
- 接入非笛卡尔晶格与几何曲面流动数据，核验样本、变量、单位、网格坐标及许可。
- 接入非结构网格与任意域PDE数据，核验样本、变量、单位、网格坐标及许可。
- 接入非配对LES与DNS湍流数据，核验样本、变量、单位、网格坐标及许可。
- 接入风场尾迹状态与反馈控制数据，核验样本、变量、单位、网格坐标及许可。
- 接入高分辨率规则网格PDE数据，核验样本、变量、单位、网格坐标及许可。
- 接入高维流场快照时序数据，核验样本、变量、单位、网格坐标及许可。
- 接入高超声速冷壁与跨声速机翼CFD数据，核验样本、变量、单位、网格坐标及许可。
- 接入高频与多尺度PDE配点，核验样本、变量、单位、网格坐标及许可。

## 执行 prompt（跨场景聚合去重）
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“CycleGAN无配对LES到DNS超分辨率重建”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“Deep Ritz与弱形式神经变分求解”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“DeepONet与神经Green函数算子学习”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“DrivAerNet汽车几何到阻力系数回归”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“E3等变粒子网络拉格朗日流体模拟”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“FNO规则网格参数化PDE算子学习”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“GAN与正规化流湍流降阶生成”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“LLM智能体自动配置与执行CFD仿真”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“Lie群等变网络对称PDE预测”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“Mamba状态空间神经算子时序预测”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“PINN不可压Navier-Stokes正问题求解”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“PINN湍流RANS均值流求解与闭合”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“POD与模态分解流动降阶建模”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“Transformer通用PDE算子预训练与微调”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“三维湍流Transformer多尺度时空预测”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“三维航空器目标气动响应逆向设计”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“主动学习驱动的PDE约束设计优化”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“几何编码网络跨声速翼型激波流场预测”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“分布外流动代理检测与校准预测”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“动态图网络与自适应网格物理时序模拟”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“区域分解并行PINN大域求解”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“卷积网络二维湍流LES亚格子闭合”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“可微PDE求解器与图网络混合数据同化”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“可微与生成式PDE最优控制”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“可微分物理求解与方程结构学习”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“可微物理流固耦合逆向设计”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“可迁移参数化ROM与实时状态监测”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“因子分解与多重网格FNO高效算子学习”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“图神经算子任意几何PDE学习”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“图神经算子任意域非结构网格流场预测”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“图网络翼型非结构网格RANS场预测”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“域分解与非均匀几何神经算子学习”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“基础模型跨PDE零样本时序预测”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“壁面压力剪切与积分气动力联合反演”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“多保真代理辅助翼型外形优化”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“多孔与复杂输运流动场数据驱动预测”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“多尺度Transformer汽车高分辨率气动预测”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“大规模网格Transformer三维流场预测”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“学习型网格移动与自适应有限元优化”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“学习型网格移动与自适应重网格”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“守恒图网络有限体积PDE推进”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“对流与标量混合湍流热通量预测”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“小波与局部谱核多尺度算子学习”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“工程LES等效源项与高保真校准”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“扩散与随机过程时空场概率预测”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“拉格朗日深度网络涡量与粒子轨迹预测”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“数据驱动RANS雷诺应力与涡黏闭合”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“数据驱动风场与非定常流反馈控制”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“时间并行与多时间尺度神经PDE推进”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“机翼阵风与跨声速流动主动控制”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“条件生成模型多物理与随机PDE模拟”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“核方法与生成模型翼型跨工况场预测”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“气动弹性与非定常载荷降阶响应预测”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“流匹配网络复杂流动概率代理”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“流固耦合移动网格振动时序预测”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“深度强化学习壁湍流摩阻削减”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“深度强化学习钝体尾迹闭环减阻控制”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“潜空间与神经场连续PDE算子学习”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“潜空间神经ODE连续流动动力学预测”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“点云与无网格网络复杂几何流场预测”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“物理信息扩散模型流场分布生成”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“物理信息概率网络流场不确定性反演”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“物理信息网络稀疏观测参数反演”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“物理网络可压缩高速绕流求解与激波识别”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“物理网络随机与高维PDE求解”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“物理边界与不变量约束神经算子学习”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“生成模型与智能体工程外形协同设计”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“生成模型气动外形与流场联合逆向设计”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“监督学习湍流场单快照超分辨率重建”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“硬约束PINN复杂几何边界求解”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“神经多重网格与预条件线性PDE求解”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“神经有限体积与域分解混合求解”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“神经校正器耦合粗网格CFD加速”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“神经逆算子PDE参数与边界反演”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“科学基础模型多物理算子迁移”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“稀疏壁面传感器三维湍流全场重构”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“稀疏观测PINN均值流与闭合联合反演”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“稳定性约束神经微分方程动力学学习”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“符号与稀疏物理学习控制方程发现”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“等变球面与Clifford神经算子学习”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“自动化与元学习PINN跨方程求解”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“自回归网络圆柱尾迹长时滚动预测”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“自然梯度与损失平衡PINN稳定训练”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“自编码器潜空间流动降阶时序预测”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“规则网格CNN翼型稳态流场代理预测”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“谱增强PINN高频多尺度PDE求解”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“贝叶斯PINN噪声数据概率反演”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“贝叶斯多保真气动代理与主动选样”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“跨声速气动弹性降阶响应预测”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“边界嵌入与任意阶硬约束神经场求解”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“迁移与不确定性约束汽车气动快速评估”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“迁移与多保真航空汽车气动代理”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“连续时间与随机生成神经算子预测”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“部分观测扩散同化与连续流场反演”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“随机微分与概率粗粒化湍流闭合”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“隐式神经场连续流动降阶建模”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“隐式表示与扩散桥CFD缺失区补全压缩”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“非笛卡尔晶格与曲面网络流动模拟”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“高保真汽车数据集全场与气动力联合代理”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。
- 读取{DATASET_PATH}中的{DATASET_NAME}，为“高超声速与跨声速边界层湍流代理”建立数据清单。检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可，按{DATA_CONTRACT}输出机器可读契约。缺少必填输入时返回BLOCKED并列出缺项，不得编造数据、权重、工况或结果。

## 输入槽（var/hint/default）
- {DATASET_PATH} | required=True | type=doc | var_name=数据集路径 | hint=目录或清单文件 | default=
- {DATASET_NAME} | required=True | type=str | var_name=数据集名称 | hint=来源与数据版本 | default=稀疏或缺损CFD场与坐标查询
- {DATA_CONTRACT} | required=False | type=object | var_name=数据契约 | hint=变量单位网格定义 | default={'input_fields': [], 'target_fields': [], 'units': {}, 'coordinates': 'dataset_native'}

## 产出
- data_audit.md
- data_contract.json
- dataset_manifest.json

## 质量门禁 quality_gate
- 不存在训练测试泄漏
- 数据文件可读且样本可追溯
- 输入目标变量单位坐标定义完整

## 可调资源（edge:resource，仅真实存在）
- datapipes/airfrans
- datapipes/deepcfd
- datapipes/deepmind_cylinderflow
- datasets/deepcfd
- models/deeponet
- models/fno
- models/meshgraphnet
- models/transformer
- models/u_net
- tools/fluidsim

## 实例任务（本骨架在各场景的实例化）
- it-056c8790
- it-08134815
- it-0d9e8f9c
- it-11397850
- it-140cca27
- it-1508066d
- it-15dd796d
- it-1f5445d9
- it-2700bd7c
- it-28a62135
