# 圆柱绕流数据接入与预处理

## 适用范围
圆柱绕流非定常时序数据集的接入、契约核验、质控、归一化/无量纲化处理，以及按几何或轨迹为单位的无泄漏数据切分。适用于自回归CFD预测场景的前处理阶段。

## 输入
- 数据集路径（{DATASET_PATH}）：目录或清单文件
- 数据集名称（{DATASET_NAME}）：来源与版本信息
- 数据契约（{DATA_CONTRACT}）：变量、单位、网格定义（可选，默认为空结构）
- 切分配置（{SPLIT_CONFIG}）：比例、随机种子、分组方式
- 目标变量列表（{TARGET_FIELDS}）：待预测物理量
- 是否无量纲化（{NONDIMENSIONALIZE}，默认true）

## 输出
- 数据集清单（dataset_manifest.json）
- 数据契约（data_contract.json）
- 数据审计报告（data_audit.md）
- 训练集清单（train_manifest.json）
- 验证集清单（validation_manifest.json）
- 测试集清单（test_manifest.json）
- 归一化统计量（normalization.json）

## 流程节点
```
s01 数据接入与契约核验
  │  操作：读取数据集，检查文件可读性、样本数、输入与目标变量、单位、坐标系、网格拓扑、时间或工况范围、缺失值和使用许可
  │  参数：{DATASET_PATH}, {DATASET_NAME}, {DATA_CONTRACT}
  │  质量门禁：数据文件可读且样本可追溯；输入目标变量单位坐标定义完整；不存在训练测试泄漏
  ▼
s02 预处理与数据切分
  │  操作：依据s01契约完成质控、重采样或图构建、掩膜、归一化或无量纲化；按{SPLIT_CONFIG}以几何、完整轨迹或物理工况为单位切分
  │  参数：{SPLIT_CONFIG}, {TARGET_FIELDS}, {NONDIMENSIONALIZE}
  │  质量门禁：三份切分的对象轨迹互斥；仅用训练集计算变换统计量；边界与掩膜语义未破坏
```

## 关键参数

### 通用判据
| 参数 | 值 | 来源 | 说明 |
|------|-----|------|------|
| 切分方式 | 按几何、完整轨迹或物理工况为单位 | 场景需求书s02 | 不得把同一轨迹的帧随机打散 |
| 变换统计量来源 | 仅训练集 | 场景需求书s02 | 防止数据泄漏 |
| 无量纲化 | 默认开启 | 场景需求书s02 | 统一跨工况量纲 |

### 校准数值
| 参数 | 值 | 来源 | 说明 |
|------|-----|------|------|
| train/val/test比例 | 0.7/0.15/0.15 | 场景需求书s02 | 以下数值来自场景默认配置，其他体系需以自身证据重新锚定 |
| 随机种子 | 42 | 场景需求书s02 | |

## 边界与分流
- 缺少必填输入时返回BLOCKED并列出缺项，不得编造数据
- 数据契约字段不完整时需补全后才能进入s02
- 切分后若发现轨迹泄漏需重新切分
- 无量纲化失败时回退到原始量纲并标注

## 质量检查
- 数据文件可读且样本可追溯
- 输入目标变量单位坐标定义完整
- 不存在训练测试泄漏
- 三份切分的对象轨迹互斥
- 仅用训练集计算变换统计量
- 边界与掩膜语义未破坏

## 回退策略
- 数据文件不可读时检查路径或格式
- 切分比例不满足需求时调整{SPLIT_CONFIG}
- 归一化统计量异常时检查数据质量

## 资源召回建议
- 本卡片为任务级卡片，覆盖工作流步骤s01和s02
- 配套工作流卡片：cfd-cylinder-wake-prediction-workflow
- 下游任务卡片：cfd-autoregressive-model-training
