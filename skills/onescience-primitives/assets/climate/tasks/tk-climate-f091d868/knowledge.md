# 骨架任务：致灾因子和事件标签时空对齐与样本构造

- domain: climate
- 复用场景数: 9
- 实例任务数: 9

## 步骤描述（跨场景聚合去重）
- 执行“致灾因子和事件标签时空对齐与样本构造”，统一DEM、河网、土地覆盖、土壤、历史洪水样点的时间、空间、单位、质量标志和缺测处理，形成可复现输入。
- 执行“致灾因子和事件标签时空对齐与样本构造”，统一卫星或摄像影像、温湿烟气传感器的时间、空间、单位、质量标志和缺测处理，形成可复现输入。
- 执行“致灾因子和事件标签时空对齐与样本构造”，统一季节气候异常、燃料与植被状态、历史火点的时间、空间、单位、质量标志和缺测处理，形成可复现输入。
- 执行“致灾因子和事件标签时空对齐与样本构造”，统一小时雨量历史、降雨预报、滑坡目录、易发性图的时间、空间、单位、质量标志和缺测处理，形成可复现输入。
- 执行“致灾因子和事件标签时空对齐与样本构造”，统一气象预报、气溶胶观测、地表与沙尘历史的时间、空间、单位、质量标志和缺测处理，形成可复现输入。
- 执行“致灾因子和事件标签时空对齐与样本构造”，统一海啸浮标、海平面与地壳形变观测的时间、空间、单位、质量标志和缺测处理，形成可复现输入。
- 执行“致灾因子和事件标签时空对齐与样本构造”，统一积雪状态、天气历史与预报、地形的时间、空间、单位、质量标志和缺测处理，形成可复现输入。
- 执行“致灾因子和事件标签时空对齐与样本构造”，统一近期天气、土壤、植被、地形和人类活动特征的时间、空间、单位、质量标志和缺测处理，形成可复现输入。
- 执行“致灾因子和事件标签时空对齐与样本构造”，统一风压预报、天文潮、历史水位、海岸地形的时间、空间、单位、质量标志和缺测处理，形成可复现输入。

## 执行 prompt（跨场景聚合去重）
- 依据{ALIGNMENT_CONFIG}、{EVENT_DEFINITION}、{IMBALANCE_POLICY}完成致灾因子和事件标签时空对齐与样本构造。对所有资料执行时空对齐、单位转换、掩膜和缺测处理，记录每一项转换前后形状、范围与时间轴；不得用验证期或目标时刻之后的信息补齐输入。
- 依据{EVENT_DEFINITION}、{ALIGNMENT_CONFIG}、{IMBALANCE_POLICY}完成致灾因子和事件标签时空对齐与样本构造。对所有资料执行时空对齐、单位转换、掩膜和缺测处理，记录每一项转换前后形状、范围与时间轴；不得用验证期或目标时刻之后的信息补齐输入。
- 依据{STATIC_FEATURE_CONFIG}、{SPATIAL_SPLIT_PROTOCOL}、{IMBALANCE_POLICY}完成致灾因子和事件标签时空对齐与样本构造。对所有资料执行时空对齐、单位转换、掩膜和缺测处理，记录每一项转换前后形状、范围与时间轴；不得用验证期或目标时刻之后的信息补齐输入。

## 输入槽（var/hint/default）
- {EVENT_DEFINITION} | required=True | type=str | var_name=事件定义 | hint=输入事件判定规则。 | default=None
- {ALIGNMENT_CONFIG} | required=True | type=str | var_name=时空对齐配置 | hint=输入时空对齐配置。 | default=None
- {IMBALANCE_POLICY} | required=True | type=str | var_name=样本不平衡规则 | hint=输入不平衡处理规则。 | default=None
- {STATIC_FEATURE_CONFIG} | required=True | type=str | var_name=静态因子配置 | hint=输入静态因子派生配置。 | default=None
- {SPATIAL_SPLIT_PROTOCOL} | required=True | type=str | var_name=空间分块协议 | hint=输入空间分块验证协议。 | default=None

## 产出
- 对齐后的标准输入
- 掩膜与样本索引
- 预处理转换记录

## 质量门禁 quality_gate
- 不存在由验证资料或未来资料造成的信息泄漏
- 正负样本构造保留真实发生率并记录抽样策略
- 每项插值、归一化、掩膜和缺测处理均有记录
- 预处理后的时间轴、坐标、单位和形状可检查

## 可调资源（edge:resource，仅真实存在）
- （域内暂无匹配资源卡——资源缺口，不编造）

## 实例任务（本骨架在各场景的实例化）
- it-6fbab14c
- it-765860b3
- it-775ecb1c
- it-87e10ded
- it-99d482e3
- it-9bd4e1b4
- it-af040f57
- it-d30c4ff1
- it-ffc75c9d

## 复用场景
- E22
- E11
- E76
- E51
- E92
- E37
- E32
- E70
- E90
