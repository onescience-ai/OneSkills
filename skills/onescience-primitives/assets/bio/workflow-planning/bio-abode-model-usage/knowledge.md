# AbODE模型使用知识缺口记录

## 适用范围
本卡片记录AbODE模型的使用知识缺口，该模型在归因报告中被提及为基于图偏微分方程的抗体生成模型。由于公开文献证据有限，本卡片仅记录已知信息和知识缺口，不提供完整的使用指南。

## 输入
- 抗原-抗体复合物特征（归因报告提及）
- 模型权重文件（abode.ckpt，状态为simulated）
- PyTorch环境和依赖库

## 输出
- 生成的抗体序列和结构（归因报告提及）

## 流程节点
1. **环境准备** → 安装PyTorch和相关依赖库（具体版本要求未知）
2. **权重加载** → 加载abode.ckpt权重文件（文件格式和加载方式未知）
3. **模型推理** → 调用AbODE API进行推理（输入输出接口未知）
4. **结果解析** → 解析生成的抗体序列和结构（输出格式未知）

## 关键参数
### 通用判据
| 参数 | 值 | 来源 | 说明 |
|------|-----|------|------|
| 模型类型 | 图偏微分方程 | 归因报告 | 基于图神经网络的生成模型 |
| 权重文件 | abode.ckpt | 归因报告 | PyTorch checkpoint格式 |
| 输入数据 | 抗原-抗体复合物特征 | 归因报告 | 具体格式未知 |
| 输出数据 | 抗体序列和结构 | 归因报告 | 具体格式未知 |

### 校准数值
由于缺乏公开文献证据，无法提供校准数值。

## 边界与分流
- **当无法获取AbODE权重时**：使用其他抗体生成模型（如RFdiffusion、ProteinMPNN）
- **当环境配置失败时**：检查PyTorch版本兼容性，参考类似模型的安装指南
- **当推理结果异常时**：验证输入数据格式，检查权重文件完整性

## 质量检查
- 验证点：模型加载成功、推理输出合理、CDR-RMSD计算正常
- 阈值：无法确定具体阈值（证据有限）
- 失败处理：回退到其他抗体生成方法

## 回退策略
- 当AbODE模型不可用时：使用RFdiffusion或ProteinMPNN进行抗体设计
- 当权重文件损坏时：重新下载或联系模型开发者获取

## 资源召回建议
- 何时应召回本卡片：需要使用AbODE模型进行抗体生成时
- 配套资源：RFdiffusion、ProteinMPNN、其他抗体生成模型

## 补充证据（证据有限说明）
本卡片基于归因报告中的描述，AbODE模型被提及为基于图偏微分方程的抗体生成模型，需要PyTorch环境和特定权重文件。然而，在检索的8篇核心论文和52篇相关论文中，均未找到关于AbODE模型的具体实现细节、权重文件来源或调用接口的公开文献证据。

可能的解释：
1. AbODE模型可能是较新的或未广泛发布的模型
2. 模型可能处于预印本阶段或内部开发阶段
3. 模型名称可能有误或指代其他模型

建议：在使用AbODE模型前，需进一步确认模型来源、获取权重文件、验证调用接口。

## 证据来源
由于缺乏公开文献证据，本卡片不包含论文引用。所有信息来源于归因报告中的描述。