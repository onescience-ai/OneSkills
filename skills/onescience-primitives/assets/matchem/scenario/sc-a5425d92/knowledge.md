# 场景：CO2_Cu111_12C13C_MACE_LAMMPS动力学验证

- domain: matchem
- type: customer_scenario
- 算力: ['VASP', 'LAMMPS'] (is_one_hpc=[True, True])
- 模型: ['mace']
- 工具: []

## 研究意图（agent_task_prompt）
你是本任务的材料化学与分子动力学负责人。请围绕 CO2/Cu(111)/水/K 界面的 12C/13C 比较，完成一项可复现、可审计的短时动力学验证任务。先核对用户提供的 C12/C13 结构、结构优化状态、历史动力学数据、训练数据、候选势能模型、集群环境与资源条件，明确哪些输入可以作为正式数据、哪些仅能作为参考。随后自主提出并说明数据准备、势能模型训练或复用判断、独立测试、短时动力学方案、固定底部两层 Cu、轨迹分析和异常处理的完整方案。短时测试目标为每种同位素建立 2 ps 的可用性证据；不得将 2 ps 轨迹或历史结果包装为同位素效应的正式结论。只有在结构、模型、元素映射、固定原子映射、晶胞表示、能量温度行为、轨迹和续算能力均通过质量检查后，才可建议长时采样。不得把对话中的旧输入、旧脚本、旧模型或旧作业结果直接当作本次结果；涉及研究条件、数据范围、训练阈值、预算、长时目标或验收标准的关键选择须先请求用户确认。最终提供数据血缘、模型测试、短时动力学质量报告、比较结果、限制说明和完整复现材料，并对每项交付标注 PASS、PARTIAL、REJECT 或 BLOCKED。

## 客户端请求
- task_title: CO2/Cu(111)/水/K 界面 12C/13C 短时动力学验证
- request: 对用户提供的 CO2/Cu(111)/水/K 界面 C12/C13 体系开展短时动力学验证。以已确认可用的结构和可追溯训练数据为基础，完成候选势能模型的独立验证与 3×3 面内扩胞短时动力学测试；原始晶胞的底部两层共 32 个 Cu 原子应固定，扩胞后须保持等价固定约束。短时测试目标为每种同位素 2 ps，先不把其视为长时生产模拟或同位素效应定量结论。
- scientific_context: 研究 CO2 在 Cu(111)/水/K 周期性界面中的局域结构与热运动，并在除碳同位素质量外保持关键条件一致的前提下，为后续比较 12C 与 13C 的动力学和结构统计建立可信的机器学习势与动力学采样基础。当前场景重点验证模型、约束和短时动力学是否可靠；同位素效应的正式定量结论需要额外的充分采样。
- desired_outcome: 得到可加载且经独立验证的势能模型或明确的不可用结论，得到 C12/C13 的 3×3 面内扩胞 2 ps 动力学可用性证据，并提供固定 Cu 映射、晶胞/元素映射、温度能量轨迹、续算能力、异常诊断和后续长时采样建议。
- executor_role: 执行者应具备材料化学、机器学习势、分子动力学和 HPC 作业诊断能力，能够主动核对结构、训练数据、模型格式、运行环境和输入一致性，并在提交前完成端到端预检。

## 问题与适用性
本对象是提交给材料化学执行者的完整需求书。它规定研究目标、科学约束、验收口径和交付要求，不预先写死具体参数、命令、资源或执行方案；执行者须先提出方案并经确认后执行。

## 工作流步骤（→workflow/→tasks）
- s01 核验结构与可用资料
- s02 验证候选势能模型可用性
- s03 准备超胞与动力学测试
- s04 执行 2 ps 短时验证
- s05 形成验证结论与后续建议

## 关联论文
- （源场景未提供）

## 验收与缺失信息策略
- acceptance_decision: （源场景未提供）
- missing_information_policy: （源场景未提供）
