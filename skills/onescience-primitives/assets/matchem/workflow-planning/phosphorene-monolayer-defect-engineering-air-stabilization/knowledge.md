# 磷烯单层缺陷工程与空气稳定化（磷烯单层缺陷工程与空气稳定化）

## 适用范围

**适用场景**：
- 研究目标、材料对象和关键科学问题与本场景一致或可经确认后等价映射。
- 可获得足以支撑目标结论的输入、数据、方法和资源，且能建立验证证据链。

**不适用场景**：
- 任务的研究对象、目标性质、关键约束或验收口径发生实质变化且未重新评估。
- 缺少支撑正式结论的关键输入、可追溯数据或验证条件，且用户不允许补充。

## 研究对象与输入

**研究对象**：磷烯单层缺陷工程与空气稳定化

## 核心科学问题

1. “磷烯单层缺陷工程与空气稳定化”所关注的关键结构、性质、性能或机制问题是什么，哪些可由本次任务直接回答？
2. 在给定研究对象和约束下，结果是否具有足够的验证证据、适用范围和不确定度说明？
3. 哪些观察可以形成结论，哪些因数据、模型、采样或资源限制只能标记为探索性结果或无法回答？

## 工作流

### 步骤 1：缺陷模型与对照定义

**目的**：生成母体和缺陷候选并固定浓度/边界。

| 参数 | 变量名 | 类型 | 必填 | 默认值 | 说明 |
|---|---|---|---|---|---|
| 母体结构 | `{HOST_STRUCTURE}` | doc | 是 | `CIF/POSCAR` | CIF/POSCAR、晶面、层数或样品信 |
| 缺陷候选 | `{DEFECT_SET}` | list[str] | 是 | ["specified defects"] | 空位、掺杂、边缘、晶界或辐照缺陷及浓度。 |

**输出**：缺陷结构集、参考态说明

**质量门禁**：
- 缺陷浓度与超胞一致
- 电荷补偿明确

**操作指令**：`根据 {HOST_STRUCTURE} 和 {DEFECT_SET} 建立缺陷候选，定义对照和电荷/化学势参考。`

### 步骤 2：结构电子或功能响应获取

**目的**：计算或测量缺陷形成、迁移、光学或催化响应。

**依赖步骤**：s01

| 参数 | 变量名 | 类型 | 必填 | 默认值 | 说明 |
|---|---|---|---|---|---|
| 环境与表征条件 | `{ENVIRONMENT}` | object | 否 | {"temperature_K": 298} | 温度、气氛、电位、光照、应力或表征方案。 |

**输出**：原始响应数据、缺陷特征

**质量门禁**：
- 参考态和校正项记录
- 对照样齐全

**操作指令**：`在 {ENVIRONMENT} 下获取响应数据，保留软件/仪器、参数和原始文件。`

### 步骤 3：缺陷功能与稳定性排序

**目的**：比较目标功能、副作用和环境稳定性。

**依赖步骤**：s02

| 参数 | 变量名 | 类型 | 必填 | 默认值 | 说明 |
|---|---|---|---|---|---|
| 环境与表征条件 | `{ENVIRONMENT}` | object | 否 | {"temperature_K": 298} | 温度、气氛、电位、光照、应力或表征方案。 |

**输出**：候选排序、风险与证据缺口

**质量门禁**：
- 不将形成能直接等同于实际浓度
- 适用域明确

**操作指令**：`对缺陷候选排序，区分热力学可行性、动力学可达性和实验可制备性。`

### 步骤 4：验证与输出

**目的**：提出可操作的制备或表征验证路线。

**依赖步骤**：s03

**输出**：验证计划、最终结论

**质量门禁**：
- 推荐具备可追溯原始证据
- 未验证项单列

**操作指令**：`输出优先缺陷方案、验证表征和 PASS/REJECT/BLOCKED 结论。`

## 验收标准与质量门禁

### 硬约束（不可违反）

- 未经确认不得改变用户给定的研究对象、科学问题、关键比较条件或边界约束。
- 关联论文、历史数据和参考模型只能作为背景或交叉核验，不能冒充本次任务的新结果。
- 不能用缺失数据、未验证模型、无效采样或未经证实的假设补足科学结论。

### 最低证据要求

- 实际研究对象、输入和关键约束与经确认的任务范围一致，所有偏离均有记录。
- 结果的数据来源、处理过程、方法选择、质量检查和适用范围可追溯。
- 每项核心结果均给出定义、单位、样本范围或分析窗口、验证证据和不确定度说明。
- 最终报告明确区分已证实结论、探索性观察、限制和无法回答的问题。

**验收判定**：执行者必须对每项交付标注 PASS、PARTIAL、REJECT 或 BLOCKED，并提供可核查的文件、日志、数据、图表或数值证据。

## 交付物清单

### 最小交付物

1. 经确认的任务方案、变更记录和执行说明。
2. 输入与数据来源说明、结果数据、质量报告和科学结论。
3. 复现材料：输入、脚本或流程说明、环境与版本信息、日志和结果清单。

### 失败交付要求

任务未完成时仍须交付最后一个可复用的健康产物、失败位置、根因证据、影响范围、已排除原因和下一步建议；失败结果不得包装为正式结论。

## 边界与限制

**不属于默认交付**：
- 场景不预先指定具体软件、命令、参数、计算资源或目录结构；这些由执行者提出并经确认。
- 场景不接受仅给出结果文件而没有数据来源、验证证据、限制说明和复现信息的交付。

## 执行前确认事项

- 研究对象、输入数据和关键科学条件是否完整、一致且可用于任务。
- 目标、可用数据、理论或实验路线、资源预算与验收标准是否相容。
- 哪些结果可以作为正式结论，哪些仅可作为探索性观察。

**信息缺失策略**：关键科学口径、输入或资源信息缺失时，执行者必须说明缺口、影响和最小补充信息；未经确认不得以默认值替代或启动高成本任务。

## 必要结果项

- **primary_result**：与场景研究目标直接对应的结构、性质、性能、筛选排序或机制分析结果。
- **evidence_result**：输入来源、数据血缘、方法选择依据、验证结果、质量检查和适用性说明。
- **结论结果**：区分已证实结果、探索性观察、不确定度、限制条件和无法回答问题的结论报告。

## 关联文献

1. **Producing air-stable monolayers of phosphorene and their defect engineering** — [doi_10_1038_ncomms10450](https://doi.org/10.1038/ncomms10450)
