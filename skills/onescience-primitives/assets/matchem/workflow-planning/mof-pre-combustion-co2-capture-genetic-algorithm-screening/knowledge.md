# MOF燃烧前CO2捕集遗传算法筛选（MOF燃烧前CO2捕集遗传算法筛选）

## 适用范围

**适用场景**：
- 研究目标、材料对象和关键科学问题与本场景一致或可经确认后等价映射。
- 可获得足以支撑目标结论的输入、数据、方法和资源，且能建立验证证据链。

**不适用场景**：
- 任务的研究对象、目标性质、关键约束或验收口径发生实质变化且未重新评估。
- 缺少支撑正式结论的关键输入、可追溯数据或验证条件，且用户不允许补充。

## 研究对象与输入

**研究对象**：MOF燃烧前CO2捕集遗传算法筛选

## 核心科学问题

1. “MOF燃烧前CO2捕集遗传算法筛选”所关注的关键结构、性质、性能或机制问题是什么，哪些可由本次任务直接回答？
2. 在给定研究对象和约束下，结果是否具有足够的验证证据、适用范围和不确定度说明？
3. 哪些观察可以形成结论，哪些因数据、模型、采样或资源限制只能标记为探索性结果或无法回答？

## 工作流

### 步骤 1：结构与分离目标定义

**目的**：确认孔道或膜结构及目标分离/捕集指标。

| 参数 | 变量名 | 类型 | 必填 | 默认值 | 说明 |
|---|---|---|---|---|---|
| 多孔材料或膜结构 | `{MATERIAL_MODEL}` | doc | 是 | `CIF/膜结构参数` | 给出孔道、层间距、表面官能团、厚度和来源 |
| 进料与运行条件 | `{FEED_AND_OPERATION}` | object | 是 | {"feed": "specified mixture", "temperature_K": 298} | 组分、浓度、湿度、压力、温度、流量或电场 |

**输出**：结构与工况清单、目标指标

**质量门禁**：
- 孔道和边界条件明确
- 进料组成与单位一致

**操作指令**：`读取 {MATERIAL_MODEL} 与 {FEED_AND_OPERATION}，确定目标组分、竞争组分和性能指标。`

### 步骤 2：传质或吸附性能获取

**目的**：计算或测量吸附、扩散、渗透和选择性。

**依赖步骤**：s01

| 参数 | 变量名 | 类型 | 必填 | 默认值 | 说明 |
|---|---|---|---|---|---|
| 评价方法 | `{EVALUATION_CONFIG}` | object | 否 | {"method": "user-confirmed"} | 说明吸附、扩散、渗透、分离或实验测试方法 |

**输出**：原始性能数据、方法日志

**质量门禁**：
- 平衡与稳态判据明确
- 空白和对照样可追溯

**操作指令**：`按 {EVALUATION_CONFIG} 获取原始性能数据，记录模型、仪器、平衡时间和对照。`

### 步骤 3：选择性稳定性与能耗分析

**目的**：在真实进料约束下比较选择性、通量、循环稳定性和能耗。

**依赖步骤**：s02

| 参数 | 变量名 | 类型 | 必填 | 默认值 | 说明 |
|---|---|---|---|---|---|
| 进料与运行条件 | `{FEED_AND_OPERATION}` | object | 是 | {"feed": "specified mixture", "temperature_K": 298} | 组分、浓度、湿度、压力、温度、流量或电场 |

**输出**：性能权衡图、失效风险清单

**质量门禁**：
- 不将单组分吸附直接等同于混合物分离
- 未测稳定性明确标注

**操作指令**：`根据 {FEED_AND_OPERATION} 分析竞争吸附、污染、湿度或机械稳定性，输出权衡关系。`

### 步骤 4：材料或工艺窗口输出

**目的**：提出材料结构与运行条件的可执行组合。

**依赖步骤**：s03

**输出**：推荐窗口、验证计划、最终结论

**质量门禁**：
- 推荐可回溯到原始数据
- 不确定性明确

**操作指令**：`输出推荐材料/膜和操作窗口，给出验证优先级和 PASS/REJECT/BLOCKED。`

## 验收标准与质量门禁

### 硬约束（不可违反）

- 未经确认不得改变用户给定的研究对象、科学问题、关键比较条件或边界约束。
- 关联论文、历史数据和参考模型只能作为背景或交叉核验，不能冒充本次任务的新结果。
- 不能用缺失数据、未验证模型、无效采样或未经证实的假设补足科学结论。

### 最低证据要求

- 实际研究对象、输入和关键约束与经确认的任务范围一致，所有偏离均有记录。
- 结果的数据来源、处理过程、方法选择、质量检查和适用范围可追溯。
- 每项核心结果均给出定义、单位、样本范围或分析窗口、验证证据和不确定度说明。
- 最终报告明确区分已证实结论、探索性观察、限制和无法回答的问题。

**验收判定**：执行者必须对每项交付标注 PASS、PARTIAL、REJECT 或 BLOCKED，并提供可核查的文件、日志、数据、图表或数值证据。

## 交付物清单

### 最小交付物

1. 经确认的任务方案、变更记录和执行说明。
2. 输入与数据来源说明、结果数据、质量报告和科学结论。
3. 复现材料：输入、脚本或流程说明、环境与版本信息、日志和结果清单。

### 失败交付要求

任务未完成时仍须交付最后一个可复用的健康产物、失败位置、根因证据、影响范围、已排除原因和下一步建议；失败结果不得包装为正式结论。

## 边界与限制

**不属于默认交付**：
- 场景不预先指定具体软件、命令、参数、计算资源或目录结构；这些由执行者提出并经确认。
- 场景不接受仅给出结果文件而没有数据来源、验证证据、限制说明和复现信息的交付。

## 执行前确认事项

- 研究对象、输入数据和关键科学条件是否完整、一致且可用于任务。
- 目标、可用数据、理论或实验路线、资源预算与验收标准是否相容。
- 哪些结果可以作为正式结论，哪些仅可作为探索性观察。

**信息缺失策略**：关键科学口径、输入或资源信息缺失时，执行者必须说明缺口、影响和最小补充信息；未经确认不得以默认值替代或启动高成本任务。

## 必要结果项

- **primary_result**：与场景研究目标直接对应的结构、性质、性能、筛选排序或机制分析结果。
- **evidence_result**：输入来源、数据血缘、方法选择依据、验证结果、质量检查和适用性说明。
- **结论结果**：区分已证实结果、探索性观察、不确定度、限制条件和无法回答问题的结论报告。

## 关联文献

1. **In silico discovery of metal-organic frameworks for precombustion CO 2 capture using a genetic algorithm** — [doi_10_1126_sciadv_1600909](https://doi.org/10.1126/sciadv.1600909)
